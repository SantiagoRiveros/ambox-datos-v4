Hasta ahora trabajamos con :

-Python
-Pandas
-CSV
-SQL

En entornos reales, los datos rara vez se encuentran en la misma fuente.
Normalmente estan almacenados en:

-MySQL
-PostgreSQL
-SQL Server
-Oracle
-SQLite
Por eso es importante aprender a conectar python con las bases de datos.

DB(database) -> SQLAlchemy -> Pandas -> Analisis de datos

¿Que es SQLAlchemy?
Es una libreria que permite:
-Conectarse a bases de datos.
-Ejecutar Consultas SQL
-Insertar datos
-Actualizar datos
-Eliminar datos
Desde -> PYTHON <-

Instalacion: 

pip install sqlalchemy

Si trabajamos con MySQL:
pip install pymysql

Solo por hoy, vamos a usar SQLite
-Viene integrado a python
-No requiere Instalacion
-Es simple para aprender


cursor.execute("""
CREATE TABLE empleados(
    id INTEGER PRIMARY KEY,
    nombre TEXT,
    edad INTEGER,
    salario REAL
)
               """) 

               cursor.execute("""
INSERT INTO empleados
VALUES
(1,'Juan',25,1500),
(2,'Ana',30,2500)
""")